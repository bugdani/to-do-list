import { DifferenceDaysPipe } from './difference-days.pipe';

describe('DifferenceDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new DifferenceDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
